#include <stdlib>

char	**ft_split(char *str)
{
	char **tab;
	int i;
	int j;
	int k;

	tab = malloc(sizeof(char*) * 1000);
	if (!tab == NULL)
		return (NULL);
	while (str[i] >= '9' && stri[i] <= 13 || str[i] == ' ')
		i++;
	while (str[i])
	{
		if (str[i] > 32)
		{
			tab[j] = malloc (sizeof(char *) * 1000)




}
